apiVersion: apps/v1
kind: Deployment
metadata:
  name: boi-transaction-service
  labels: { app: boi-transaction-service }
spec:
  replicas: 1
  selector:
    matchLabels: { app: boi-transaction-service }
  template:
    metadata:
      labels: { app: boi-transaction-service }
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: boi-transaction-service
          image: PLACEHOLDER_IMAGE   # patched per-overlay / by Jenkins
          ports:
            - containerPort: 8083
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
          resources:
            requests: { cpu: "100m", memory: "256Mi" }
            limits:   { cpu: "500m", memory: "512Mi" }
          livenessProbe:
            httpGet: { path: /actuator/health/liveness, port: 8083 }
            initialDelaySeconds: 40
            periodSeconds: 10
            failureThreshold: 3
          readinessProbe:
            httpGet: { path: /actuator/health/readiness, port: 8083 }
            initialDelaySeconds: 30
            periodSeconds: 10
            failureThreshold: 3
---
apiVersion: v1
kind: Service
metadata:
  name: boi-transaction-service
  labels: { app: boi-transaction-service }
spec:
  selector: { app: boi-transaction-service }
  ports:
    - port: 8083
      targetPort: 8083
  type: ClusterIP
