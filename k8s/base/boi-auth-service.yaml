apiVersion: apps/v1
kind: Deployment
metadata:
  name: boi-auth-service
  labels: { app: boi-auth-service }
spec:
  replicas: 1
  selector:
    matchLabels: { app: boi-auth-service }
  template:
    metadata:
      labels: { app: boi-auth-service }
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: boi-auth-service
          image: PLACEHOLDER_IMAGE   # patched per-overlay / by Jenkins
          ports:
            - containerPort: 8081
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
          resources:
            requests: { cpu: "100m", memory: "256Mi" }
            limits:   { cpu: "500m", memory: "512Mi" }
          livenessProbe:
            httpGet: { path: /actuator/health/liveness, port: 8081 }
            initialDelaySeconds: 40
            periodSeconds: 10
            failureThreshold: 3
          readinessProbe:
            httpGet: { path: /actuator/health/readiness, port: 8081 }
            initialDelaySeconds: 30
            periodSeconds: 10
            failureThreshold: 3
---
apiVersion: v1
kind: Service
metadata:
  name: boi-auth-service
  labels: { app: boi-auth-service }
spec:
  selector: { app: boi-auth-service }
  ports:
    - port: 8081
      targetPort: 8081
  type: ClusterIP
